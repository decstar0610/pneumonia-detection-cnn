# PneumoScan API — runtime deps (TensorFlow-free; fits Render's 512MB free tier).
# The DenseNet121 backbone runs in onnxruntime; the head + Grad-CAM run in NumPy.
onnxruntime==1.28.0
numpy==1.26.4
pillow==10.4.0
fastapi==0.112.1
uvicorn[standard]==0.30.6
python-multipart==0.0.9
huggingface_hub==1.25.1   # used at Docker build time to fetch model artifacts
